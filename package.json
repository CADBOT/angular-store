{
  "version": "0.0.2",
  "name": "angular-store",
  "description": "A demo storefront angular app",
  "repository": "https://github.com/cadbot/angular-store",
  "devDependencies": {
    "http-server": "^0.6.1",
    "tmp": "0.0.23",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8000"
  }
}
